# Small architecture-check tuning grid. Run with Job_tune_global.sbatch.
# The base params use 5 offline datasets, 4 benchmark points, and about 120
# offline transition rows per dataset.
combos:
  - name: base
    overrides: {}

  - name: kernel_length_1p0
    overrides:
      kernel:
        length_scale: 1.0
