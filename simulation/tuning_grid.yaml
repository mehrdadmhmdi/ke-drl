# Pair-specific final grid. The active SIM2_POLICY_PAIR profile is defined in
# params_tune.yaml under policy_pair_overrides and is applied before these
# optional local overrides.
combos:
  - name: active_pair_profile
    overrides: {}
